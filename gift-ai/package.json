{
  "name": "gift-ai",
  "private": true,
  "workspaces": [
    "backend",
    "telegram-bot",
    "dashboard"
  ],
  "scripts": {
    "dev": "npm run dev -w backend",
    "dev:dashboard": "npm run dev -w dashboard",
    "dev:bot": "npm run dev -w telegram-bot",
    "build": "npm run build -w backend && npm run build -w telegram-bot && npm run build -w dashboard",
    "start": "npm run start -w backend",
    "seed": "npm run seed -w backend"
  }
}
