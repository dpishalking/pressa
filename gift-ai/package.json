{
  "name": "gift-ai",
  "private": true,
  "workspaces": [
    "backend",
    "telegram-bot"
  ],
  "scripts": {
    "dev": "npm run dev -w backend",
    "dev:bot": "npm run dev -w telegram-bot",
    "build": "npm run build -w backend && npm run build -w telegram-bot",
    "start": "npm run start -w backend",
    "seed": "npm run seed -w backend"
  }
}
