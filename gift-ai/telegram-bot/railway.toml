[build]
builder = "nixpacks"
buildCommand = "npm install && test $(ls assets/mascot/*.jpg | wc -l | tr -d ' ') -ge 25"

[deploy]
startCommand = "npm run start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
numReplicas = 1
